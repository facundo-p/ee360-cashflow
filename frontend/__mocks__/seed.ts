// Entrada única de seeds para el mock UI.
import { usuariosSeed } from './usuarios';
import { mediosSeed } from './medios';
import { tiposSeed } from './tipos';
import { movimientosSeed } from './movimientos';

export const seed = {
  usuarios: usuariosSeed,
  medios: mediosSeed,
  tipos: tiposSeed,
  movimientos: movimientosSeed,
};


